import { style } from "@vanilla-extract/css";

export const root = style({
  paddingTop: 4 * 4,
});

export const title = style({
  fontSize: 44,
  fontWeight: "bold",
  marginBottom: 10 * 4,
  textAlign: "center",
});
